
import { fromJS } from 'immutable';
import contactListReducer from '../reducer';
import {
  DEFAULT_ACTION, INITIAL_CONTACTS
} from '../constants';

describe('contactListReducer', () => {
  it('returns the initial state');
});
