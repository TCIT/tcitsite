// import { fromJS } from 'immutable';
// import { makeSelectMedicineSearchDomain } from '../selectors';

// const selector = makeSelectMedicineSearchDomain();

describe('makeSelectMedicineSearchDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
