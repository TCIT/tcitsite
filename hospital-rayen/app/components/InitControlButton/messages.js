/*
 * InitControlButton Messages
 *
 * This contains all the text for the InitControlButton component.
 */
import { defineMessages } from 'react-intl';

export default defineMessages({
  header: {
    id: 'app.components.InitControlButton.header',
    defaultMessage: 'Iniciar Control',
  },
});
